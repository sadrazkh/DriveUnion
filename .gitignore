# .NET
bin/
obj/
*.user
[Dd]ebug/
[Rr]elease/
tests/**/bin/
tests/**/obj/

# Frontend. Both are produced by npm during the build — the font is copied out of the pinned
# vazirmatn package, the bundles are Vite's output — so neither is source.
node_modules/
src/DriveUnion.Web/wwwroot/build/
src/DriveUnion.Web/wwwroot/fonts/

# Secrets / local
.env
*.log
appsettings.*.local.json
# Where the Google OAuth client the operator types into the panel is written. The secret in it is
# encrypted, the client id is not, and neither belongs in the repository.
src/DriveUnion.Web/App_Data/

# OS / IDE
.vs/
.idea/
.DS_Store
Thumbs.db

# Per-agent isolated build outputs (dotnet build --artifacts-path)
/.artifacts/
