@{
    @* Resolves to Views/Shared/_Layout.cshtml: /Views/Shared/{0}.cshtml is the last of the area
       view locations, so these pages get the panel's own shell rather than a second one. *@
    Layout = "_Layout";
}
